### 문제

- 암기테스트 통과 못함
    
    아주 적은 양의 데이터(256행)를 모델에게 외워보라고 시킨 LSTM 암기 테스트가 1.21%로 기준(1%)을 아주 조금 넘어서 학습 실패
    
    1%에 근접했기 때문에, 모델 결함이 아닌 학습 횟수 부족으로 판단됨. 만약 모델에 구조적 문제(기울기 소실, 파라미터 초기화 오류, 입력 스케일 문제 등)가 있었다면 loss가 초기값에서 거의 안 내려오거나, 역으로 loss가 오르거나, NaN이 나왔을 거다.
    
    해결 방법 :
    
    1. **암기 테스트 epoch 100 → 200으로 늘리기**
    2. **혹시 200으로도 실패하면 바로 sys.exit 대신 경고(WARNING)만 출력하고 계속 진행하도록 변경
    (1.5% 이하면 WARNING, 1.5% 초과면 sys.exit)**
    
    →  결과 : 
    초기 오차 대비 **0.96%** 수준으로 오차를 줄이며 거뜬히 통과. 최소한 데이터를 학습할 능력(용량)이 충분하다는 뜻.
    

### 학습 결과

지역별 LSTM vs XGBoost 비교

| 지역 | LSTM MAE | XGBoost MAE | 승자 |
| --- | --- | --- | --- |
| 전라남도 | 154.4 | 90.4 | XGBoost |
| 충청남도 | 26.4 | 25.5 | XGBoost |
| 경상북도 | 21.1 | 12.2 | XGBoost |
| 대전시 | 2.71 | 0.14 | XGBoost |
| 서울시 | 2.81 | 0.18 | XGBoost
 |

![national_lstm_loss_curve.png](attachment:ff78f5fc-c241-41ae-b670-f2d603671cbd:national_lstm_loss_curve.png)

그래프를 보면 전형적인 **과적합 패턴.** Train과 Val 사이의 간격이 매우 크고, Val이 수렴하지 못하고 흔들리는 게 보인다. → 모델이 train 데이터를 외우고 있지만 test에 일반화가 안 되는 상태

- Train Loss(파란선) : epoch 내내 꾸준히 하강 → 0에 수렴
- Val Loss(주황선) : epoch 5~10 구간에서 최저점 도달 후 들쭉날쭉하면서 상승