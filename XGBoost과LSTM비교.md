### XGBoost 학습 조건

1. 에폭 500회
2. 하이퍼파라미터
    - 트리 개수 (n_estimators): 500
    - 트리 최대 깊이 (max_depth): 6
    - 학습률 (learning_rate): 0.05
    - 데이터 샘플링 비율 (subsample): 0.8
    - 피처 샘플링 비율 (colsample_bytree): 0.8
    - 재현성을 위한 시드값 (random_state): 42
3. 정제된 데이터 : 80만 행
4. 추출된 피처 : 24개 (타겟까지 포함)

### **전라남도에서 유독 LSTM이 예측 상태가 안 좋은 이유**

1. 압도적인 수치를 잡으려고 애쓰다보니 AI가 전남의 거대한 변동성을 억지로 쫓아가려다 오히려 '과적합(Overfitting)'되거나 큰 실수를 저지름.
2. LSTM같은 신경망 모델은 불확실한 상황에서는 보통 평균값을 낸다고 한다. 데이터가 들쭉날쭉이라면 평균값을 내다보니 그만큼 오차가 심해질 수 있음
3. LSTM은 과거 패턴을 파악함으로써 미래를 예측하는데, 만약 전남 기상 변화가 불규칙해서 과거 패턴을 찾기 힘들어했다면 복잡한 계산을 할 수 있는 LSTM이 어제값을 그대로 들고오는 Naive보다 상태가 더 안좋을 수 있다.

### **LSTM 과적합 원인**

![national_lstm_loss_curve.png](attachment:a380d1d8-57ac-46e5-9bde-2efdc9908b28:national_lstm_loss_curve.png)

모델이 train 데이터를 거의 암기했지만 새로운 데이터(2023년 test)에 일반화하지 못하고 있다.

1. **스케일 불균형:** 
    
    전라남도(최대 ~1,000 MWh)와 서울(최대 ~3 MWh)이 동일한 StandardScaler로 정규화된다. 스케일 차이가 20~300배인 지역이 섞인 상태에서 MSELoss를 쓰면 서울 같은 조그만 애들은 좀 틀려도 티도 안 나니, 덩치 큰 전남만 죽어라 파자!"라고 판단해서 거기에 맞춘다.
    
    서울, 대전 등에서 LSTM의 MAE가 XGBoost보다 10~20배나 높은 이유가 여기에 있다. AI에게 서울의 데이터는 "줄여야 할 오차(Loss)가 너무 작아서 무시해도 되는 정보"로 취급받아, 정교하게 학습되지 않고 대충 평균적인 값(평활화)으로 내뱉게 된 것
    
    StandardScaler : 모든 지역의 데이터를 '평균 0, 표준편차 1'로 맞추는 작업. 상대적인 비율만 맞출 뿐, 절대적인 수치의 힘을 완전히 없애지는 못한다.
    
2. **시퀀스 오염 가능성:** seq_len=24로 지역별 시퀀스를 만들 때, train 마지막 20%를 validation으로 쓰면 2022년 말 데이터가 val에 들어간다. 하지만 2022년 하반기와 2023년(test) 사이의 분포 차이가 크면, val loss가 test 성능을 대표하지 못한다.
3. **데이터 구조 불일치:**
    
    XGBoost : “만약 지역 번호가 13번(전남)이고, 어제 발전량이 100 이상이면 → A값 출력” 하는 식으로 스위치를 켜고 끄듯(Branching) 명확하게 구분한다. 지역코드라는 숫자가 들어오면 트리 구조에서 즉시 "아, 이건 전남용 계산기로 가야지"라고 길을 나누기가 매우 쉬워서 이질적인 데이터들을 따로따로 처리하는 데 구조적으로 최적화되어 있다.
    
    LSTM : LSTM은 17개 지역의 데이터를 하나의 긴 '시퀀스(연속된 흐름)'로 봅니다. 지역이 바뀔 때마다 맥락이 완전히 달라지는데, LSTM은 이를 하나의 일관된 논리로 이해하려고 애를 쓴다. 서로 성격이 너무 다른 17개 지역의 데이터를 한 줄로 세워 학습시키다 보니, 모델이 "이 이야기가 갑자기 왜 이렇게 바뀌지?"라며 혼란을 겪게 된다. 
    

### ESS 평가 기준 바꾸기

문제 : DEMAND_MWH_PER_HOUR=50를 전지역에 동일하게 적용하면 안된다. 전남은 공급 > 수요이기 때문에 남아도는 전기를 어떻게 효율적으로 저장하냐가 관건이라 50이란 값은 알맞지 않다. 반면 서울은 공급 < 수요라서 항상 전기가 모자라다. 수요 50보다 훨씬 클 것이다.

**새로운 발견 :**

- XGBoost(MAE 9.66)가 LSTM(MAE 17.82)보다 84% 정확하지만,
ESS 운영의 핵심 지표인 부족횟수는 LSTM(426회)이 XGBoost(515회)보다 17% 적었다.
이는 정확도 메트릭과 다운스트림 운영 성과가 분리될 수 있음을 실증한다. 단순히 오차를 줄이자가 아닌, 현실적인 사용 기준에 맞춰 모델 선택 및 학습이 필요함을 보여준다.
- 근데 반대로 로직에 의해 LSTM이 더 유리하게 작용한 걸 수도 있다.
예측이 음수(방전 결정)인데 실제로는 양수(발전이 수요보다 많음)인 경우, `needed`는 0이 돼서 방전 안 한다.
근데 반대로 예측이 양수(충전)인데 실제로는 음수(부족)인 경우, 충전 분기로 들어가서 부족 카운트를 안 해서 부족 사태가 누락되는 거다. `shortage_count`는 방전 분기에서만 카운트되니까.
    
    → XGBoost와 LSTM의 부족횟수 차이(515 vs 426)가 진짜 운영 품질 차이가 아니라 "예측이 어느 방향으로 틀렸는가" 차이일 수도 있다. 보수적으로 (낮게) 예측한 모델이 방전 분기에 더 자주 들어가서 부족을 더 잘 카운트하는 식으로.
    

이 발견이 사실 포트폴리오의 진짜 시그니처가 될 수 있음. "단순히 ESS 점수가 다르다고 LSTM이 우월하다고 결론 내리지 않았고, 시뮬레이터 로직 자체를 검증해서 그 차이가 어디서 오는지 추적했다" 얘기. 이게 면접에서 강력하다고 함.